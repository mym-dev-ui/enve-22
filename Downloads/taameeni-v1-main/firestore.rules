rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow full read/write during development — tighten before production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
